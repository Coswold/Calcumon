<div class = "content">
      <!-- navbar (settings, level, dashboard) -->
      {{navbar}}

      <div class = "problem-content">
        <!-- place to dispay question -->
        <h1 id = "problem" class = "problem"> Sample Problem: What is 5 + 5? </h1>

        <!-- canvas (for drawing calcumons and animating-->
        <canvas class = "game"></canvas>
        <!-- place for answers -->
        <input id = "userInput" class = "solution" placeholder="Enter solution">
        <input id = "submit" class = "solution-submit button" type="submit" value="Submit">
        <button id = "skip" class="button">skip</button>
      </div>
      <!-- attacks (buttons) -->
      <div>

      </div>
    </div>
    <script src="../utils/problemGenerator.js"></script>
    
    <script>
      // these scripts are temporary until game object classes are connected

      // function that gets and displays new problem
      function displayNewProblem() {
        problem = getProblem(1, 0, true)
        document.getElementById('problem').value = makeProblemReadable(problem)
      }
      // Gets the player input from the problem-solution form when solution is submitted by player
      // Remove from here when running through game class's update() function
      let submit = document.getElementById('submit')
      let userInput = null
      submit.onclick = function() {
          userInput = document.getElementById('userInput').value
          alert(userInput)
          // send this to player class...?
          // do verification etc.
      }

    </script>